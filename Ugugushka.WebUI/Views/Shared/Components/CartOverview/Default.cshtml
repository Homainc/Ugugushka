@model Ugugushka.Domain.DtoModels.Cart
<table class="table">
    <thead>
    <tr>
        <th></th>
        <th>Кол-во</th>
        <th>Цена</th>
        <th>Сумма</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var line in Model.Lines)
    {
        <tr>
            <td>@line.Toy.Name</td>
            <td>@line.Quantity</td>
            <td>@line.Toy.Price руб.</td>
            <td>@(line.Quantity * line.Toy.Price) руб.</td>
        </tr>
    }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3">Доставка курьером</td>
            <td id="tableCourierPrice"><span></span> руб.</td>
        </tr>
        <tr>
            <td colspan="3">Итого</td>
            <td><span id="tableTotalValue">@Model.ComputeTotalValue()</span> руб.</td>
        </tr>
    </tfoot>
</table>